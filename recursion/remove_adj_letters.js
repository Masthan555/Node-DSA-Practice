/* Masthan 00000 */

let remAdjChars = (str, i, j, res)=>{
    if(i>=j) return;

    if(str[i] == str[b]){
        
    }

    if(res.length>0){
        if(res[res.length-1] == str[i]){

        }
    }
}